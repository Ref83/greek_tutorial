# Vocabulary read page

The page should consist of the text from the `story` divided by `sentences`
For each sentence when user click on it there is a `translation`
On the top of the page should be the button `Listen` that play the `story_block_<N>.mp3` file. 